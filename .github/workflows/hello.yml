name: Hello world!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
    - name: Say hello
      run: echo "Hello world!"
    - name: Run date
      run: date
    - name: List directory content
      run: ls -l
